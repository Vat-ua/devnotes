export const meta = {
  slug: "do-zero-a-um-site-estatico-com-react",
  category: "Fundamentos",
  title: "Do zero a um site estático com React",
  excerpt: "Crie um pequeno site com Vite, três páginas, rotas e um layout compartilhado — sem CMS, sem framework de CSS e sem uma arquitetura inflada.",
  readTime: "14 min de leitura",
  date: "2026-09-01",
  accent: "lime",
}

Um site estático não precisa ser um arquivo HTML enorme nem uma aplicação React com camadas demais. Para um projeto pequeno, o objetivo é simples: ter uma estrutura que seja fácil de entender hoje e fácil de ampliar quando uma nova página aparecer.

Neste guia, vamos criar uma base com três páginas: **Início**, **Serviços** e **Sobre**. Cada rota representa uma página da aplicação, mas o projeto continua sendo uma SPA: o navegador carrega o mesmo documento HTML e o React troca a view exibida.

## 1. Crie o projeto com Vite

Comece com o template React em JavaScript e instale apenas o roteador. Não há necessidade de biblioteca de componentes ou framework de CSS para esta base.

No terminal, na pasta onde você guarda seus projetos, execute:

```bash
# terminal
pnpm create vite meu-site --template react
cd meu-site
pnpm install
pnpm add react-router
pnpm dev
```

O Vite entrega o servidor local, o build de produção e uma configuração pequena. Antes de mudar qualquer arquivo, abra a URL mostrada no terminal e confirme que o projeto inicial funciona.

## 2. Dê nomes claros às responsabilidades

Crie uma estrutura suficiente para diferenciar páginas, componentes compartilhados e estilos. Não crie uma pasta para cada ideia hipotética.

Ao final desta etapa, a pasta `src/` terá esta organização:

```text
# src/
src/
  components/
    Header.jsx
    Footer.jsx
  layouts/
    AppLayout.jsx
  pages/
    home.jsx
    servicos.jsx
    sobre.jsx
  styles/
    global.css
  App.jsx
  main.jsx
```

Uma página representa uma rota. Um componente compartilhado existe porque aparece em mais de um lugar. Um layout junta a moldura que todas as páginas dividem.

## 3. Inicialize o router

No ponto de entrada, envolva a aplicação com `BrowserRouter`. Em um host estático, lembre-se de configurar fallback para `index.html` quando for publicar; durante o desenvolvimento, o Vite já cuida disso.

Substitua o conteúdo de `src/main.jsx`:

```jsx
// src/main.jsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router";
import App from "./App.jsx";
import "./styles/global.css";

createRoot(document.getElementById("root")).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
);
```

## 4. Declare as páginas em um só lugar

`App.jsx` deve explicar a navegação sem esconder o mapa do site. O `AppLayout` envolve as páginas e renderiza cada uma na posição de `Outlet`.

Substitua o conteúdo de `src/App.jsx`:

```jsx
// src/App.jsx
import { Route, Routes } from "react-router";
import AppLayout from "./layouts/AppLayout.jsx";
import Home from "./pages/home.jsx";
import Servicos from "./pages/servicos.jsx";
import Sobre from "./pages/sobre.jsx";

export default function App() {
  return (
    <Routes>
      <Route element={<AppLayout />}>
        <Route index element={<Home />} />
        <Route path="servicos" element={<Servicos />} />
        <Route path="sobre" element={<Sobre />} />
      </Route>
    </Routes>
  );
}
```

Crie uma primeira versão para cada página importada pelas rotas:

```jsx
// src/pages/home.jsx
export default function Home() {
  return <main className="page"><h1>Início</h1></main>;
}
```

```jsx
// src/pages/servicos.jsx
export default function Servicos() {
  return <main className="page"><h1>Serviços</h1></main>;
}
```

```jsx
// src/pages/sobre.jsx
export default function Sobre() {
  return <main className="page"><h1>Sobre</h1></main>;
}
```

## 5. Compartilhe a moldura, não o conteúdo

Header e Footer são partes da estrutura do produto; o conteúdo de cada página continua local à sua rota. Esse limite deixa a aplicação previsível.

Crie `src/layouts/AppLayout.jsx`:

```jsx
// src/layouts/AppLayout.jsx
import { Outlet } from "react-router";
import Header from "../components/Header.jsx";
import Footer from "../components/Footer.jsx";

export default function AppLayout() {
  return (
    <div className="app-shell">
      <Header />
      <Outlet />
      <Footer />
    </div>
  );
}
```

No Header, use `NavLink` em vez de links soltos: ele permite identificar a rota ativa sem manter outro estado apenas para isso.

Crie `src/components/Header.jsx`:

```jsx
// src/components/Header.jsx
import { NavLink } from "react-router";

const navigation = [
  ["Início", "/"],
  ["Serviços", "/servicos"],
  ["Sobre", "/sobre"],
];

export default function Header() {
  return (
    <header className="site-header">
      <div className="container header-inner">
        <NavLink className="site-brand" to="/">Meu site</NavLink>
        <nav className="site-nav" aria-label="Principal">
          {navigation.map(([label, to]) => (
            <NavLink key={to} to={to} end={to === "/"}>
              {label}
            </NavLink>
          ))}
        </nav>
      </div>
    </header>
  );
}
```

`end={to === "/"}` impede que **Início** fique ativo também em `/servicos` e `/sobre`. Sem `end`, o React Router considera `/` o início de qualquer rota.

Crie também `src/components/Footer.jsx`, usado pelo layout:

```jsx
// src/components/Footer.jsx
export default function Footer() {
  return (
    <footer className="site-footer">
      <div className="container">Meu site</div>
    </footer>
  );
}
```

## 6. Dê acabamento à estrutura compartilhada

O objetivo aqui não é criar um design system completo. É dar ao exemplo uma base visual utilizável: largura de leitura, cabeçalho, navegação, rodapé, páginas e um estado ativo claro. Cada valor abaixo é usado por uma regra real.

Crie `src/styles/global.css`:

```css
/* src/styles/global.css */
:root {
  --bg: #f5f3ed;
  --surface: #fffefa;
  --text: #151614;
  --muted: #6e6d66;
  --line: rgba(21, 22, 20, 0.15);
  --container: 1180px;
  font-family: system-ui, sans-serif;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  background: var(--bg);
  color: var(--text);
  line-height: 1.5;
}

a {
  color: inherit;
}

.app-shell {
  min-height: 100svh;
  display: flex;
  flex-direction: column;
}

.app-shell > main {
  flex: 1;
}

.container,
.page {
  width: min(100% - 2rem, var(--container));
  margin-inline: auto;
}

.site-header {
  border-bottom: 1px solid var(--line);
  background: var(--surface);
}

.header-inner,
.site-nav,
.site-footer {
  display: flex;
  align-items: center;
}

.header-inner {
  min-height: 72px;
  justify-content: space-between;
  gap: 1rem;
}

.site-brand,
.site-nav a {
  font-weight: 700;
  text-decoration: none;
}

.site-nav {
  gap: 0.25rem;
}

.site-nav a {
  padding: 0.5rem 0.75rem;
  border-radius: 0.5rem;
  color: var(--muted);
}

.site-nav a:hover,
.site-nav a.active {
  background: var(--bg);
  color: var(--text);
}

.page {
  padding-block: 4rem;
}

.page h1 {
  margin-top: 0;
}

.site-footer {
  min-height: 80px;
  border-top: 1px solid var(--line);
  color: var(--muted);
}

:focus-visible {
  outline: 3px solid rebeccapurple;
  outline-offset: 4px;
}

@media (max-width: 560px) {
  .header-inner {
    padding-block: 1rem;
    flex-direction: column;
    align-items: stretch;
  }

  .site-nav a {
    flex: 1;
    text-align: center;
  }
}
```

Em telas estreitas, o media query empilha o cabeçalho e distribui os três links pelo espaço disponível. Para apenas três páginas, isso é uma navegação responsiva suficiente. Um menu com botão só passa a valer a pena quando houver mais links ou uma razão real para escondê-los.

## 7. Escreva o conteúdo de uma página

Cada rota é um componente React comum. Nesta etapa, vamos transformar a página de Serviços em uma seção estática com uma estrutura semântica simples.

Substitua o conteúdo de `src/pages/servicos.jsx` por uma seção semântica:

```jsx
// src/pages/servicos.jsx
export default function Servicos() {
  return (
    <main className="page">
      <h1>Serviços</h1>
      <section>
        <h2>Estratégia, design e desenvolvimento</h2>
        <p>
          Ajudamos equipes a transformar uma ideia em uma experiência digital clara.
        </p>
      </section>
    </main>
  );
}
```

O mesmo padrão vale para Início e Sobre: cada página pode ter seu próprio conteúdo, mantendo Header, Footer e navegação compartilhados pelo layout.

## 8. Faça o build antes de considerar pronto

O servidor de desenvolvimento é ótimo para trabalhar, mas o build confirma que imports, rotas e assets podem ser entregues como arquivos estáticos.

No terminal, na raiz do projeto, execute:

```bash
# terminal
pnpm run build
pnpm run preview
```

Abra Início, Serviços e Sobre na prévia. Teste também uma rota acessada diretamente e a navegação em uma tela estreita. Esse pequeno ritual encontra problemas que um componente isolado não mostra.
